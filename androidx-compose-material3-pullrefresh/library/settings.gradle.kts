pluginManagement {
    repositories {
        gradlePluginPortal()
    }

    plugins {
        id("de.fayard.refreshVersions") version "0.60.0"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}