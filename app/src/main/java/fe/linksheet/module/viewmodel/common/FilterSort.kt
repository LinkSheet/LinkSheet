package fe.linksheet.module.viewmodel.common

enum class SortType {
    AZ,
    InstallTime,
}

enum class StateModeFilter {
    ShowAll,
    EnabledOnly,
    DisabledOnly
}

enum class TypeFilter {
    All,
    Browser,
    Native
}
